export class Pixel {
  x: number;
  y: number;
  temperature: number;

  constructor(x: number, y: number, temperature: number) {
    this.x = x;
    this.y = y;
    this.temperature = temperature;
  }
}
